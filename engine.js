function normalize(s){return s.normalize('NFKC').toLowerCase().replace(/配送员|送餐人员|骑士/g,'骑手').replace(/商家/g,'商户').replace(/消费者/g,'用户').replace(/交易总额/g,'交易额').replace(/除以|÷/g,'/').replace(/加上/g,'+').replace(/减去/g,'-').replace(/分钟|min/g,'分钟').replace(/到|至/g,'~').replace(/[^\p{L}\p{N}+/%~.-]/gu,'');}
function similarity(input,standard){const a=normalize(input),b=normalize(standard);if(!a)return 0;if(a===b)return 100;let prev=Array.from({length:b.length+1},(_,i)=>i);for(let i=1;i<=a.length;i++){let row=[i];for(let j=1;j<=b.length;j++)row[j]=Math.min(row[j-1]+1,prev[j]+1,prev[j-1]+(a[i-1]===b[j-1]?0:1));prev=row;}return Math.max(0,100*(1-prev[b.length]/Math.max(a.length,b.length)));}
function shuffle(a){a=[...a];for(let i=a.length-1;i>0;i--){const r=new Uint32Array(1);crypto.getRandomValues(r);const j=Math.floor(r[0]/4294967296*(i+1));[a[i],a[j]]=[a[j],a[i]];}return a;}
function makeExam(bank){const cats=shuffle([...new Set(bank.map(q=>q.category))]);const pools=cats.map(c=>shuffle(bank.filter(q=>q.category===c)));return Array.from({length:20},(_,i)=>pools[i%3].pop());}
